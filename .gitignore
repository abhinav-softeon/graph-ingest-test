.env
/.venv/

/venv/

/.pytest_cache/

__pycache__/
**/__pycache__/
*.py[cod]

graph.joblib
*.joblib
.cache/
.graph_checkpoints/

# Cython build output (setup_cython.py) — platform-specific, rebuilt per
# environment (e.g. inside Docker), never committed.
*.so
*.pyd
build/

# Runtime logs/reports generated during local runs

runs/index.jsonl

# Local ad-hoc analysis/audit outputs
run_audit.py
ACCURACY_AUDIT.md